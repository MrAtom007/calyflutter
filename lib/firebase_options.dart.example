// TEMPLATE - copia questo file in `lib/firebase_options.dart` ed esegui
// `flutterfire configure` per generarlo con le chiavi del TUO progetto Firebase.
//
// Il file reale `lib/firebase_options.dart` è ignorato da git perché contiene
// le API key client del progetto. Senza di esso l'app funziona comunque in
// locale (l'inizializzazione Firebase fallisce ed è intercettata in main.dart),
// ma il salvataggio cloud dei progressi resta disattivato.
//
// ignore_for_file: type=lint
import 'package:firebase_core/firebase_core.dart' show FirebaseOptions;

class DefaultFirebaseOptions {
  static FirebaseOptions get currentPlatform {
    throw UnsupportedError(
      'Firebase non configurato. Esegui `flutterfire configure` per generare '
      'lib/firebase_options.dart con le chiavi del tuo progetto.',
    );
  }
}
